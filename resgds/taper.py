#!/usr/bin/env python

import scipy.constants as sp
import numpy as np

Z0 = 50
ZL = 34
D0 = 4
f0 = 7.3e-09
wavelength = sp.c / f0
L = D0*wavelength

dz = L / 10000
z1 = 
z=[z1, z2]

a = 1/z(end)*log(ZL/Z0)
Z_exp = Z0*np.exp(a*z)
gamma_exp_mod = .5*np.log(ZL/Z0)*abs(np.sin(2*np.pi*D))./(2*np.pi*D);

